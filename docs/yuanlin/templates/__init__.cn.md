# __init__.py (templates)

## 概述
`freqtrade/templates/__init__.py` 是 templates 包的入口文件，内容为空。该目录作为 Freqtrade 模板文件的存放位置，包含示例策略、超参数优化损失函数等 Python 模板文件，以及 Jinja2 模板文件（用于生成新策略和配置）。

## 依赖关系

### 内部依赖（本项目模块）
无

### 外部依赖（第三方库）
无

### 被依赖（谁引用了本文件）
无直接引用（该包通过 `PackageLoader` 被 `template_renderer.py` 间接使用）
