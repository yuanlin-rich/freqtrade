``` output
usage: freqtrade install-ui [-h] [--erase] [--prerelease]
                            [--ui-version UI_VERSION]

options:
  -h, --help            显示此帮助信息并退出
  --erase               清理 UI 文件夹，不下载新版本。
  --prerelease          安装最新的 FreqUI 预发布版本。不建议用于生产环境。
  --ui-version UI_VERSION
                        指定要安装的 FreqUI 特定版本。
                        不指定此项将安装最新版本。

```
