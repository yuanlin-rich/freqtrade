``` output
usage: freqtrade create-userdir [-h] [--userdir PATH] [--reset]

options:
  -h, --help            显示此帮助信息并退出
  --userdir, --user-data-dir PATH
                        用户数据目录的路径。
  --reset               将示例文件重置为其原始状态。

```
